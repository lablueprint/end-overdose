'use client';

import Character from '../Character';
import styles from '../../game.module.css';

const Fire = () => {
    return (
        <div className={styles.fire}>
            <Character />
        </div>
    );
};

export default Fire;
