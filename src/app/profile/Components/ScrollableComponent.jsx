const ScrollableComponent = (props) => {
    return (names = props.names
        const listItems = names.map(names.map((name) => {
            <li> {name} </li>
        }))
    );
};

export default ScrollableComponent;
